import Image from "next/image";

const Output = async () => {
  return (
    <>
      {/* <div>
        {image.map((item) => (
          <div key={item._id}>
            <Image
              src={`/${item.image}`}
              width={200}
              height={200}
              alt="image"
            />
          </div>
        ))}
      </div> */}
    </>
  );
};

export default Output;
